package com.example.socialite;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeEventEndpointIT extends EventEndpointTest {

    // Execute the same tests but in native mode.
}