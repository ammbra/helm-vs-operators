apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "event.fullname" . }}-test-connection"
  labels:
{{ include "event.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "event.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
